{
  "version": "1.0",
  "id": "check-group-builder",
  "name": "CheckGroupsPage",
  "states": [
    {
      "id": "cg-list-management",
      "name": "Check Group Library Management",
      "requiredElements": [
        {
          "text": "Check Groups"
        },
        {
          "role": "button",
          "text": "New"
        },
        {
          "role": "searchbox"
        }
      ],
      "description": "The left panel presents all saved check groups in a searchable list following the standard library builder layout. Users can browse existing groups, create new ones, and select a group to load into the editor. Each list item shows the group name, the number of checks it contains, and any tags. The list supports search filtering to help users find specific groups in large libraries. Creating a new group opens a blank editor with default settings (sequential execution, stop on failure).",
      "provenance": {
        "source": "migrated"
      }
    },
    {
      "id": "cg-check-composition",
      "name": "Check Composition and Ordering",
      "requiredElements": [
        {
          "text": "Check Groups"
        },
        {
          "text": "Check Groups"
        },
        {
          "text": "Check Groups"
        },
        {
          "role": "button",
          "text": "Save"
        }
      ],
      "description": "The core function of the check group editor is composing a group from existing checks in the library. Users see a list of available checks they can add to the group, and a list of currently included checks showing their order. The check selector shows check names, types, and tool information so users can identify the right checks. Users can add and remove individual checks from the group, building up a verification suite that covers all necessary validation points.",
      "provenance": {
        "source": "migrated"
      }
    },
    {
      "id": "cg-execution-config",
      "name": "Execution Configuration",
      "requiredElements": [
        {
          "text": "Check Groups"
        },
        {
          "text": "Check Groups"
        },
        {
          "text": "Check Groups"
        },
        {
          "text": "Check Groups"
        }
      ],
      "description": "Check groups support configuring how their constituent checks are executed. Users can choose between sequential execution (checks run one after another) and parallel execution (checks run concurrently for faster verification). The stop-on-failure flag controls whether the group halts at the first failing check or continues to run all checks to collect complete results. These options let users balance between execution speed, early failure detection, and comprehensive reporting depending on their workflow needs.",
      "provenance": {
        "source": "migrated"
      }
    }
  ],
  "transitions": [],
  "provenance": {
    "source": "migrated"
  },
  "description": "Check Group Builder page -- a specialized editor for creating and managing check groups that bundle multiple individual checks into reusable test suites. Check groups allow users to compose verification steps from existing checks, controlling execution order, parallelism, and failure behavior. When used in a workflow verification phase, a check group runs all its constituent checks as a single verification unit, simplifying workflow configuration and promoting consistent verification patterns.",
  "metadata": {
    "tags": [
      "check-groups",
      "verification",
      "builder",
      "library",
      "test-suites"
    ]
  }
}
