# ProductivityPage

Interim three-panel page (plan `2026-09-12-consolidate-local-orchestration-onto-conductor`, Phase 4):
Knowledge (inline browser), File activity, Overlapping intents. No sub-views, no modal
trigger (Ctrl+Shift+E is global). Spec hand-authored from the source in the same commit;
the runner-driven generator (`scripts/generate-all-specs.ps1`) needs a live runner and was
not used.
