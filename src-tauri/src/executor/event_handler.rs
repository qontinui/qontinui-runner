// EventHandler removed as it was unused
// If needed in the future, event handling is done directly through Tauri's event system
// ExecutorEvent struct moved to python_bridge.rs where it's actually used
