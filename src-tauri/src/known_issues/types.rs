//! Known issue types for persistent, cross-run issue tracking.
//!
//! The canonical definitions live in the lib crate so the schema-export
//! pipeline can see them — see `qontinui_runner_lib::known_issue_types`.

pub use qontinui_runner_lib::known_issue_types::*;
