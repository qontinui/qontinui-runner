/**
 * Constraint Results Widget
 *
 * Standalone dashboard widget for real-time constraint evaluation results.
 * Auto-shows when constraint-results events arrive during workflow execution.
 */

export { ConstraintResultsWidget } from "./ConstraintResultsWidget";
export { useConstraintResults } from "./useConstraintResults";
export type { ConstraintResultsData, ConstraintIterationResults, ConstraintCounts } from "./types";
