{
  "version": "1.0.0",
  "description": "The Activity Timeline is a searchable history of everything captured on screen during automation. Users search by typing natural language queries (e.g., 'login button disabled') and the page returns relevance-ranked results from a PostgreSQL full-text search index. Each result shows a text preview, capture source badge, app name, timestamp, and optional URL. Data is populated automatically by three background capture sources and is filtered for PII before storage. The page serves as the primary debugging tool for answering 'what was on screen when X happened?'",
  "groups": [
    {
      "id": "timeline-navigation",
      "name": "Page Navigation",
      "description": "The Activity Timeline lives under the Observe sidebar group and is a runner-only page. Users navigate to it by clicking 'Activity Timeline' in the sidebar.",
      "category": "navigation",
      "assertions": [
        {
          "id": "timeline-nav-reachable",
          "description": "The page loads when the 'activity-timeline' tab is selected, showing the search interface",
          "category": "navigation",
          "severity": "critical",
          "target": { "type": "search", "criteria": { "placeholder": "Search activity timeline... (e.g., 'login button disabled')" }, "label": "Search input with placeholder" },
          "assertionType": "exists",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        }
      ],
      "source": "manual"
    },
    {
      "id": "timeline-semantic",
      "name": "Page Purpose",
      "description": "Users come to this page to search through captured screen content. The search input placeholder and overall layout should make it immediately clear that this is a search-first interface for screen capture history.",
      "category": "semantic",
      "assertions": [
        {
          "id": "timeline-sem-search-purpose",
          "description": "The page communicates its purpose through the search input placeholder which includes an example query ('login button disabled')",
          "category": "semantic",
          "severity": "critical",
          "target": { "type": "search", "criteria": { "placeholder": "Search activity timeline... (e.g., 'login button disabled')" }, "label": "Search input with example query" },
          "assertionType": "visible",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-sem-empty-guidance",
          "description": "The page shows 'No results found. Try a different query.' after a search with no matches, guiding users to refine their query",
          "category": "semantic",
          "severity": "warning",
          "target": { "type": "search", "criteria": { "text": "No results found. Try a different query." }, "label": "Empty results guidance" },
          "assertionType": "visible",
          "precondition": "A search has been performed that returned zero results",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        }
      ],
      "source": "manual"
    },
    {
      "id": "timeline-element-presence",
      "name": "Core UI Elements",
      "description": "The page always shows a search bar with input, Search button, filter toggle button, and stats button, regardless of whether any data exists in the timeline.",
      "category": "element-presence",
      "assertions": [
        {
          "id": "timeline-ep-search-input",
          "description": "The page shows a text input with placeholder 'Search activity timeline... (e.g., 'login button disabled')'",
          "category": "element-presence",
          "severity": "critical",
          "target": { "type": "search", "criteria": { "selector": "input[type='text'][placeholder*='Search activity timeline']" }, "label": "Search text input" },
          "assertionType": "exists",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-ep-search-button",
          "description": "The page shows a button labeled 'Search' (or 'Searching...' during a query)",
          "category": "element-presence",
          "severity": "critical",
          "target": { "type": "search", "criteria": { "role": "button", "textContains": "Search" }, "label": "Search button" },
          "assertionType": "exists",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-ep-filter-button",
          "description": "The page shows a button containing a Filter icon (lucide Filter) for toggling the filter panel",
          "category": "element-presence",
          "severity": "warning",
          "target": { "type": "search", "criteria": { "selector": "button svg.lucide-funnel" }, "label": "Filter toggle button with icon" },
          "assertionType": "exists",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-ep-stats-button",
          "description": "The page shows a button with title='Load stats' containing a Clock icon",
          "category": "element-presence",
          "severity": "info",
          "target": { "type": "search", "criteria": { "selector": "button[title='Load stats']" }, "label": "Stats button" },
          "assertionType": "exists",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-ep-source-filter-dropdown",
          "description": "The page shows a select dropdown with options 'All sources', 'UI Bridge', 'Accessibility', 'OCR'",
          "category": "element-presence",
          "severity": "warning",
          "target": { "type": "search", "criteria": { "selector": "select" }, "label": "Source filter dropdown" },
          "assertionType": "exists",
          "precondition": "The filter toggle button has been activated (showFilters=true)",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-ep-app-filter-input",
          "description": "The page shows a text input with placeholder 'App name filter'",
          "category": "element-presence",
          "severity": "warning",
          "target": { "type": "search", "criteria": { "selector": "input[placeholder='App name filter']" }, "label": "App name filter input" },
          "assertionType": "exists",
          "precondition": "The filter toggle button has been activated (showFilters=true)",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        }
      ],
      "source": "manual"
    },
    {
      "id": "timeline-interaction",
      "name": "User Interactions",
      "description": "Users interact with the page by typing queries, clicking Search (or pressing Enter), toggling filters, and loading stats. Each search invokes a Tauri IPC command that runs a PostgreSQL full-text search.",
      "category": "custom",
      "tags": ["interaction"],
      "assertions": [
        {
          "id": "timeline-int-enter-searches",
          "description": "Users can press Enter in the search input to trigger a search without clicking the button. The input has an onKeyDown handler that calls handleSearch on Enter.",
          "category": "custom",
          "severity": "critical",
          "target": { "type": "search", "criteria": { "selector": "input[placeholder*='Search activity timeline']" }, "label": "Search input" },
          "assertionType": "enabled",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-int-filter-toggle",
          "description": "Users can click the filter button to reveal a filter row containing an app name input and source type dropdown. Clicking again hides it.",
          "category": "custom",
          "severity": "warning",
          "target": { "type": "search", "criteria": { "selector": "input[placeholder='App name filter']" }, "label": "App filter visible after toggle" },
          "assertionType": "exists",
          "precondition": "The filter toggle button has been clicked once",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-int-stats-load",
          "description": "Users can click the 'Load stats' button to fetch aggregate capture counts. Results appear as colored badges showing '{sourceType} ({captureMode}): {count} entries'.",
          "category": "custom",
          "severity": "info",
          "target": { "type": "search", "criteria": { "textContains": "entries" }, "label": "Stats badge showing entry count" },
          "assertionType": "exists",
          "precondition": "The stats button has been clicked and at least one entry exists in the activity_timeline table",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        }
      ],
      "source": "manual"
    },
    {
      "id": "timeline-data-display",
      "name": "Search Result Display",
      "description": "Each search result is a bordered card showing: a source type badge (color-coded span), optional app name, timestamp (right-aligned), optional relevance rank, text preview (3-line clamp), and optional URL with ExternalLink icon. Results are ordered by PostgreSQL ts_rank descending.",
      "category": "custom",
      "tags": ["data-display"],
      "assertions": [
        {
          "id": "timeline-dd-result-card",
          "description": "Each result is rendered as a div with classes 'border border-border rounded-md p-3 mb-2 hover:bg-accent/50 transition-colors'",
          "category": "custom",
          "severity": "critical",
          "target": { "type": "search", "criteria": { "selector": ".border.border-border.rounded-md.p-3.mb-2" }, "label": "Result card container" },
          "assertionType": "exists",
          "precondition": "A search has returned at least one result",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-dd-source-badge",
          "description": "Each result card shows the source type as a badge with classes 'px-1.5 py-0.5 rounded text-xs font-medium'. Badge text is one of: 'ui_bridge', 'accessibility', 'ocr'. Colors: ui_bridge=bg-blue-500/20 text-blue-400, accessibility=bg-green-500/20 text-green-400, ocr=bg-amber-500/20 text-amber-400.",
          "category": "custom",
          "severity": "critical",
          "target": { "type": "search", "criteria": { "selector": "span.text-xs.font-medium.rounded" }, "label": "Source type badge" },
          "assertionType": "exists",
          "precondition": "A search has returned at least one result",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-dd-timestamp",
          "description": "Each result card shows a timestamp formatted via toLocaleString() in 'text-xs text-muted-foreground ml-auto' positioned to the right of the header row",
          "category": "custom",
          "severity": "critical",
          "target": { "type": "search", "criteria": { "selector": "span.text-xs.text-muted-foreground.ml-auto" }, "label": "Right-aligned timestamp" },
          "assertionType": "exists",
          "precondition": "A search has returned at least one result",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-dd-text-preview",
          "description": "Each result card shows captured text in a paragraph with classes 'text-sm text-foreground line-clamp-3', truncated to 3 visible lines",
          "category": "custom",
          "severity": "critical",
          "target": { "type": "search", "criteria": { "selector": "p.line-clamp-3" }, "label": "Text preview paragraph" },
          "assertionType": "exists",
          "precondition": "A search has returned at least one result",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-dd-rank",
          "description": "Each result card shows 'rank: {number}' where number is the PostgreSQL ts_rank relevance score formatted to 2 decimal places",
          "category": "custom",
          "severity": "info",
          "target": { "type": "search", "criteria": { "textContains": "rank:" }, "label": "Relevance rank value" },
          "assertionType": "exists",
          "precondition": "A search has returned at least one result with a non-null rank",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-dd-url",
          "description": "Results that captured a URL show it below the text preview with an ExternalLink icon (lucide) and the URL text in a truncated span",
          "category": "custom",
          "severity": "info",
          "target": { "type": "search", "criteria": { "selector": "span.truncate" }, "label": "Truncated URL display" },
          "assertionType": "exists",
          "precondition": "A search has returned a result where the captured page had a URL",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-dd-app-name",
          "description": "Results that have an app name show it as 'text-xs text-muted-foreground' next to the source badge",
          "category": "custom",
          "severity": "info",
          "target": { "type": "search", "criteria": { "selector": ".flex.items-center.gap-2 > span.text-xs.text-muted-foreground" }, "label": "App name next to badge" },
          "assertionType": "exists",
          "precondition": "A search has returned a result with a non-null appName",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        }
      ],
      "source": "manual"
    },
    {
      "id": "timeline-behavior",
      "name": "Automatic Behaviors",
      "description": "The page has automatic behaviors: the Search button shows 'Searching...' during queries, results clear on error, and the empty state only appears after a search has been performed (not on initial load).",
      "category": "custom",
      "tags": ["behavior"],
      "assertions": [
        {
          "id": "timeline-beh-loading-text",
          "description": "The Search button text changes to 'Searching...' while a search request is in-flight, then reverts to 'Search' when complete",
          "category": "custom",
          "severity": "warning",
          "target": { "type": "search", "criteria": { "role": "button", "text": "Searching..." }, "label": "Loading state button text" },
          "assertionType": "exists",
          "precondition": "A search has been triggered and the Tauri IPC call is pending",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-beh-no-premature-empty",
          "description": "The page does not show the 'No results found' message before any search has been performed. The message only appears when searched=true and results are empty.",
          "category": "custom",
          "severity": "warning",
          "target": { "type": "search", "criteria": { "text": "No results found. Try a different query." }, "label": "Empty state message" },
          "assertionType": "notExists",
          "precondition": "The page has just loaded and no search has been performed",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-beh-error-clears-results",
          "description": "If a search fails (Tauri command error), the results array is cleared to [] and no stale results are displayed",
          "category": "custom",
          "severity": "warning",
          "target": { "type": "search", "criteria": { "selector": ".border.border-border.rounded-md.p-3" }, "label": "Result card (should not exist after error)" },
          "assertionType": "notExists",
          "precondition": "A search has been performed and the backend returned an error (e.g., PostgreSQL unavailable)",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        }
      ],
      "source": "manual"
    },
    {
      "id": "timeline-state-consistency",
      "name": "State Consistency",
      "description": "The Search button's disabled state must stay in sync with the input content. Filters panel visibility must toggle correctly.",
      "category": "state-consistency",
      "assertions": [
        {
          "id": "timeline-sc-button-disabled-when-empty",
          "description": "The Search button has the disabled attribute when the search input value is empty or whitespace-only. The button has class 'disabled:opacity-50'.",
          "category": "state-consistency",
          "severity": "critical",
          "target": { "type": "search", "criteria": { "role": "button", "textContains": "Search" }, "label": "Search button" },
          "assertionType": "disabled",
          "precondition": "The search input is empty",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-sc-button-enabled-with-text",
          "description": "The Search button is enabled (not disabled) when the search input contains non-whitespace text",
          "category": "state-consistency",
          "severity": "critical",
          "target": { "type": "search", "criteria": { "role": "button", "textContains": "Search" }, "label": "Search button" },
          "assertionType": "enabled",
          "precondition": "The search input contains at least one non-whitespace character",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        },
        {
          "id": "timeline-sc-button-disabled-during-loading",
          "description": "The Search button is disabled while a search is in progress (loading=true), showing 'Searching...' text",
          "category": "state-consistency",
          "severity": "warning",
          "target": { "type": "search", "criteria": { "role": "button", "text": "Searching..." }, "label": "Search button during loading" },
          "assertionType": "disabled",
          "precondition": "A search has been triggered and is still pending",
          "source": "manual",
          "reviewed": true,
          "enabled": true
        }
      ],
      "source": "manual"
    },
    {
      "id": "timeline-architecture",
      "name": "Architecture and Data Pipeline",
      "description": "The Activity Timeline is backed by a PostgreSQL table (activity_timeline) with 6 indexes including a GIN full-text search index on to_tsvector('english', text_content). The table is created automatically on PgDb startup via ensure_tables() in database/pg/mod.rs. Queries are generated by Clorinde from queries/activity_timeline.sql with nullable field annotations. The PgDb wrapper (database/pg/activity_timeline.rs) handles SHA-256 content hashing and 30-second deduplication window via find_recent_duplicate(). Search uses PostgreSQL plainto_tsquery + ts_rank for relevance ranking, capped at 50 results with 500-char LEFT() previews. Three data sources feed the timeline: (1) BackgroundObserver (TypeScript, ui-bridge/ai/background-observer.ts) — captures UI Bridge semantic snapshots every 10 seconds with SemanticDiffManager change detection, circuit breaker after 10 consecutive errors, started from App.tsx useEffect with 5-second delay; (2) Python bridge (gui_automation.py) — emits activity_timeline_capture events after each GUI action containing real accessibility tree text (walked recursively with 200-depth guard), intercepted by EventForwarder::handle_timeline_capture() in executor/events.rs; (3) BackgroundCaptureService (Rust, recording/background_capture.rs) — tokio background task for periodic black-box desktop captures with in-memory content-hash dedup.",
      "category": "semantic",
      "assertions": [],
      "source": "manual"
    },
    {
      "id": "timeline-privacy",
      "name": "Privacy and Content Filtering",
      "description": "All text content is filtered before storage by ContentFilter (recording/content_filter.rs). PII scrubbing uses 7 regex patterns: email addresses → [EMAIL], credit card numbers (starting with 3-6) → [CREDIT_CARD], SSNs (###-##-####) → [SSN], bearer tokens → [BEARER_TOKEN], OpenAI-style sk-* keys → [API_KEY], GitHub ghp_* tokens → [GITHUB_TOKEN], and generic api_key/access_token patterns → [REDACTED_KEY]. Sensitive window detection skips capture entirely for 11 password manager applications (1Password, KeePass, KeePassXC, Bitwarden, LastPass, Dashlane, Enpass, Keeper, NordPass, Credential Manager, Keychain Access) and incognito/private browsing windows. The filter is applied in two locations: the insert_activity_entry Tauri command (commands/activity_timeline.rs) via a OnceLock<ContentFilter> singleton, and the Python event interceptor (EventForwarder::handle_timeline_capture in executor/events.rs) via a separate OnceLock instance. When a sensitive window is detected, the Tauri command returns -1 without storing.",
      "category": "semantic",
      "assertions": [],
      "source": "manual"
    },
    {
      "id": "timeline-implementation",
      "name": "Implementation Details",
      "description": "Frontend: ActivityTimelinePanel (React, components/activity-timeline/ActivityTimelinePanel.tsx) uses useState for query, results, stats, loading, searched, showFilters, appFilter, sourceFilter. Search invokes Tauri command 'search_activity_timeline' or 'search_activity_timeline_filtered' depending on filter state. Stats fetched via 'get_activity_timeline_stats'. Results rendered as bordered cards with color-coded source badges (blue=ui_bridge, green=accessibility, amber=ocr). The component is lazy-loaded in TabContent.tsx. Tab ID 'activity-timeline' registered in tab-types.ts. Sidebar item in qontinui-navigation/src/groups.ts under OBSERVE_ITEMS with Activity icon and #06B6D4 cyan color. BackgroundObserver service (services/background-observer-service.ts) wraps BackgroundObserver class, calls getGlobalRegistry().captureSnapshot() for control snapshots, and invokes insert_activity_entry Tauri command to persist captures. Hash computation uses crypto.subtle SHA-256 with FNV-1a fallback for non-secure (HTTP) contexts.",
      "category": "semantic",
      "assertions": [],
      "source": "manual"
    }
  ],
  "metadata": {
    "component": "ActivityTimelinePanel",
    "author": "Claude",
    "createdAt": "2026-03-26T00:00:00.000Z",
    "updatedAt": "2026-03-26T00:00:00.000Z",
    "pageUrl": "/observe/activity-timeline",
    "tags": ["screenpipe", "activity-timeline", "full-text-search", "capture-history", "postgresql"],
    "backendFiles": [
      "qontinui-runner/src-tauri/src/commands/activity_timeline.rs",
      "qontinui-runner/src-tauri/src/database/pg/activity_timeline.rs",
      "qontinui-runner/src-tauri/queries/activity_timeline.sql",
      "qontinui-runner/src-tauri/src/recording/content_filter.rs",
      "qontinui-runner/src-tauri/src/executor/events.rs"
    ],
    "apiEndpoints": [
      "Tauri: search_activity_timeline(query, maxResults?) -> Vec<SearchResult>",
      "Tauri: search_activity_timeline_filtered(query, appName?, sourceType?, taskRunId?, maxResults?) -> Vec<SearchResult>",
      "Tauri: get_activity_timeline_stats() -> Vec<{sourceType, captureMode, count, latestCapture}>",
      "Tauri: insert_activity_entry(input) -> i64 (called by BackgroundObserver, not by this page)"
    ]
  }
}
